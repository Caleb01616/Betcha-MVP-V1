import ChallengeCreation from '@/components/BetSlip'

export default function CreateChallengePage() {
  // This is a server component, so we'll handle user authentication in the client component
  return <ChallengeCreation />
}
